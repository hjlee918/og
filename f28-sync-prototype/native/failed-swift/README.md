# Inactive Swift attempt

`FilesystemHelper.swift` is retained only as historical evidence. It never
compiled or ran. The installed Swift compiler (`6.0.3.1.10`) rejected the
installed SDK modules built with Swift `6.0.3.1.5`. The active experiment uses
`../filesystem_helper.c`; the build does not compile this directory or bypass
the Swift compatibility check.
