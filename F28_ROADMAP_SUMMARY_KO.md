# F28 로컬 동기화 프로토타입 — 실험 요약 (한국어)

최종 갱신: 2026-09-16 · 검토 기준 체크포인트: `1fed5b3ef` ·
브랜치: `feature/f28-reference-paths`

## 이 문서의 위치

이 문서는 **F28 동기화 실험 하나에 대한 요약**입니다.
**프로젝트 전체의 권위 있는 로드맵이 아닙니다.**

- **전체 로드맵 (권위 있음):** 사용자 문서
  `project-notes/PROJECT_ROADMAP_KO.md` — 이 저장소 바깥에 있습니다.
  두 문서가 어긋나면 **그쪽이 맞습니다.**
- **계약과 증거 (권위 있음):**
  [`f28-sync-prototype/CONTRACT.md`](./f28-sync-prototype/CONTRACT.md),
  [`f28-sync-prototype/RESULTS.md`](./f28-sync-prototype/RESULTS.md).
  진행 요약과 계약이 어긋나면 **계약과 결과 문서가 맞습니다.**

이전 이름은 `PROJECT_ROADMAP_KO.md`였고, 사용자 전체 로드맵과 이름이 같아
혼동을 일으켰습니다. 그래서 이름을 바꿨습니다.

## 현재 단계

**7단계 / 전체 10단계 — 7단계의 약 50%.** 측정값이 아니라 계획 추정치이며,
**이번 작업으로 진행도는 올라가지 않았습니다.**

**이번 작업:** 감독 검토 결과를 기록하고, 보류된 일시 중지 제안을
닫았습니다. **새 구현도 새 권한도 승인되지 않았습니다.**

**다음 작업:** 수용된 앱 종료 상태 적용 방식을 기반으로 한 **다음 단계 제안
1건**을 제시합니다. 구현 여부는 별도의 범위 결정 사항입니다.

## 감독 판단 (2026-09-16)

일시 중지 설계를 **보류하고, 수용된 앱 종료 상태 적용 방식을 유지**하라는
권고를 **수용**했습니다.

**개정 2를 "안전성 검증 완료"로 표시하지 않습니다.** 미해결 지적 6건이
남아 있으며, 이번 작업에서는 **기록만 하고 재설계하지 않았습니다.**

### 미해결 지적 6건 (모두 차단 사유)

1. **승인 만료가 이미 시작된 헬퍼 작업을 멈추지 못합니다.** 헬퍼 규약에
   만료·중단·취소 명령이 **아예 없습니다.** 명령은 아홉 개뿐이고, 한 번
   실행하고 끝나는 프로그램입니다.
2. **5.4절의 "만료되면 안내로 완화"가 5.2절의 "종료 확인과 반영 확인을
   모두 마쳐야 한다"와 모순됩니다.** 또한 "그래도 편집 재개" 버튼은
   **승인되지 않은 위험 감수 선택이지, 안전하다는 증거가 아닙니다.**
3. **3.1절과 필수 시험 5번이 서로 맞지 않습니다.** 3.1절은 진행 중인 작업이
   있으면 무조건 거부하는데, 시험 5번은 진행 중인 쓰기가 있는 상태를
   요구합니다. 둘 다 필수로 표시되어 있어 서로 충돌합니다.
4. **동기적 호출만으로는 장부의 정확한 소유권이 증명되지 않습니다.**
   중복 제거로 여러 등록이 하나로 합쳐져 **어느 등록이 정리된 것인지
   불분명**하고, **기록이 만들어지기도 전에 아무것도 쓰지 않고 끝나 버리는
   경로**가 있어 그 등록은 영영 정리되지 않습니다.
5. **편집 관문이 모든 편집 변경을 덮지 못합니다.** 확실한 사례 두 가지가
   있습니다 — **할 일 상태 바꾸기**와 **블록 지우기**는 **선택된 블록**에
   대해 동작하므로, **편집 칸이 하나도 열려 있지 않은 상태**에서도 실행되고
   두 관문을 **모두 그냥 지나갑니다.** (참고로 직접 호출 자리는 다섯 파일에
   23군데이지만, **이 숫자 자체가 관문이 얼마나 덮는지를 재는 값은
   아닙니다** — 어디를 봐야 하는지 알려 줄 뿐이고, 근거는 위 두 사례입니다.)
   또한 **막아야 할 지역 변경과 반드시 허용해야 할 반영 변경을 구분하는
   규칙이 없습니다.**
6. **영속 표식 파일의 형식·보관 위치·수명·중단 순서가 정해지지 않았습니다.**
   (앞선 초안에서 "지금 도우미로는 만들 수 없다"고 적었던 것은 **사실이
   아니어서 바로잡습니다** — 도우미에는 이미 그래프 안
   `logseq/.og-sync` 폴더에 안전하게 파일을 쓰는 경로가 있습니다.)
   없는 것은 **승인된 표식 형식**, **표식 전용 보관 자리**(기존 자리들은
   신원·복구 기록이 쓰고 있어 **그것을 대신 쓰면 신원 기록이 덮어써집니다** —
   그렇게 하지 않습니다), **누가 언제 지우는지에 대한 규칙**, 그리고
   **노트 쓰기와의 순서 약속**입니다. 앞으로 어떤 방식으로 만들지는
   **열려 있고, 이 문서가 정하지 않습니다.**

## 수용된 마일스톤 (그대로 유지)

이전에 수용되었고, 이번 작업으로 약화되지 않았습니다.

1. **순수 조정 플래너와 인메모리 실행기.**
2. **합성 파일시스템 적용 실험** — 고정된 헬퍼, 승인된 테스트 루트 안에서만.
3. **읽기 전용 선택 세대 비교**와 **합성 비교/미리보기/적용 워크플로.**
4. **순수 신원 등록 및 변경 캡처**, **안정적 합성 작업 폴더 실험.**
5. **기본 비활성 OG 이벤트 브리지** — 제품 경로는 런타임을 설치하지 않습니다.
6. **테스트 전용 영속 신원·복구 레코드.**
7. **라이브 캡처 (2026-09-15).**
8. **들어오는 변경 적용, 앱 종료 상태 (2026-09-15)** — 더 새로운 지역 편집을
   조용히 덮어쓰지 않고 적용합니다. **다음 단계의 기반입니다.**
9. **제한적인 유휴 앱 관찰 결과 (2026-09-16)** — **제한된 범위에서** 수용.
10. **보류 원인의 그래프 바인딩 수정** — 수용.

### 9번·10번의 정확한 수용 범위 (변경 없음)

- 감독이 **메모리 전용 프로브 검사 15/15를 직접 실행**했습니다.
- 집중 라이브 결과 **14/14는 코더가 보고한 증거**입니다.
- **전체 라이브 관찰 행렬은 완료로 수용되지 않았습니다.**
- **동시 편집 안전성은 검증되지 않았고 주장되지 않습니다.**
- 과거 시험 보고는 **그대로 보존**하며, 새로 검증된 증거로 다시 쓰지
  않습니다.

## 범위 밖 (변경 없음)

들어오는 이름 변경과 삭제, 실제 전송, 두 번째 기기, 개인 데이터 등록,
일상 사용과 배포 승인은 범위 밖이며 요청되지 않았습니다.
