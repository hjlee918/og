# F28 로컬 동기화 프로토타입 — 한국어 로드맵

최종 갱신: 2026-09-16 · 검토 기준 체크포인트: `63e4625f5` ·
브랜치: `feature/f28-reference-paths`

이 문서는 진행 상황 요약입니다. 계약과 증거는
[`f28-sync-prototype/CONTRACT.md`](./f28-sync-prototype/CONTRACT.md)와
[`f28-sync-prototype/RESULTS.md`](./f28-sync-prototype/RESULTS.md)가
권위를 가지며, 둘이 어긋날 경우 그쪽이 맞습니다.

## 현재 단계

**7단계 / 전체 10단계 — 약 50%.** 이 수치는 측정값이 아니라 계획 추정치입니다.

**이번 작업:** 편집/저장 일시 중지 설계의 남은 데이터 손실 위험을 수정했습니다.
**설계 수정은 검토 대기 상태이며, 새로운 구현이나 권한은 승인되지 않았습니다.**

**다음 단계:** 방어 가능한 제한적 구현을 선택할지, 아니면 수용된 앱 종료 상태
적용 방식을 유지할지에 대한 감독자/사용자 결정.

## 수용된 마일스톤 (그대로 유지)

아래 항목은 이전에 수용되었고, 이번 작업으로 약화되지 않았습니다.

1. **순수 조정 플래너와 인메모리 실행기** — 지속성이나 적용 코드를 전혀
   가져오지 않습니다.
2. **합성 파일시스템 적용 실험** — 고정된 헬퍼를 통해 승인된 테스트 루트
   안에서만 동작합니다.
3. **읽기 전용 선택 세대 비교**와 **합성 비교/미리보기/적용 워크플로**.
4. **순수 신원 등록 및 변경 캡처**, **안정적 합성 작업 폴더 실험**.
5. **기본 비활성 OG 이벤트 브리지** — 제품 경로는 런타임을 설치하지 않습니다.
6. **테스트 전용 영속 신원·복구 레코드** — 두 개의 컴파일타임 루트에만
   고정됩니다.
7. **라이브 캡처 (2026-09-15)** — 실제 OG 저장/이름 변경을 영속 신원 레코드로
   캡처했습니다. 애플리케이션은 아무 권한도 얻지 않았습니다.
8. **들어오는 변경 적용, 앱 종료 상태 (2026-09-15)** — 더 새로운 로컬 편집을
   조용히 덮어쓰지 않고 적용합니다. 생성과 갱신만, 단일 호스트, 합성 데이터.
9. **제한적인 유휴 앱 관찰 결과 (2026-09-16)** — 감독자가 **제한된 범위에서**
   수용했습니다.
10. **보류 원인(pending cause)의 그래프 바인딩 수정** — 사이드카 그래프 id가
    아니라 OG 저장소 식별자에 바인딩합니다. 수용되었습니다.

### 9번과 10번에 대한 정확한 수용 범위

- 감독자가 **메모리 전용 프로브 검사 15/15를 직접 실행**했습니다.
- 집중 라이브 결과 **14/14는 여전히 코더가 보고한 증거**입니다.
- **전체 라이브 관찰 행렬은 완료로 수용되지 않았습니다.**
- **동시 편집 안전성은 검증되지 않았고 주장되지도 않습니다.**
- 주입된 레코드, 합성 `CompositionEvent`, 동일 프로세스 내 복구 호출,
  모의 보조 쓰기는 **실제 통합에 대한 주장으로 전환되지 않습니다.**

## 진행 중 — 제안 단계 (승인·구현되지 않음)

**편집/저장 일시 중지 설계**
([`EDITING_PAUSE_DESIGN.md`](./f28-sync-prototype/EDITING_PAUSE_DESIGN.md),
개정 2).

들어오는 변경 하나를 적용하기 위한 명시적인 "대기 중인 변경 적용" 명령입니다.
지속적인 동시 편집이나 일반 동기화 서비스가 아닙니다.

개정 2에서 바로잡은 내용:

- **조용한 대기 창은 완료 증명이 아닙니다.** 인정(admission)부터 정착까지
  실제 소유권 추적을 설계했습니다. 데이터베이스 트랜잭션이 큐 등록을
  동기적으로 수행하므로 이 추적에는 빈틈이 없습니다.
- **텍스트 영역 보존만으로는 편집을 안전하게 지킬 수 없습니다.** 저장되지
  않은 버퍼가 남아 있으면 적용을 **거부**합니다. 오래된 데이터베이스 상태를
  다시 큐에 넣는 방식은 철회했습니다. 한글 조합은 어떤 경우에도 강제로
  저장하거나 끝내지 않습니다.
- **타임아웃과 재시작이 미해결 적용 중에 쓰기를 다시 열어서는 안 됩니다.**
  쓰기 이전 취소와 쓰기 이후 불확실성을 분리했고, 경로 단위 차단과 사용자
  확인 탈출 경로를 두어 영구 잠금도 조용한 해제도 없도록 했습니다.
- **보호 주장 정정.** 통제되는 것, 합성 환경에서 비활성인 것, 보장 밖에 있는
  것을 분리해 명시했습니다. 게시 시점 검사는 열거된 노트 파일만 다루며,
  노트가 아닌 파일은 포함되지 않습니다.
- **프로세스 강제 종료 시 저장되지 않은 버퍼는 보존되지 않습니다.** 개정 1의
  반대 취지 서술은 철회했습니다.

## 권고

**다음 프로토타입에서는 수용된 앱 종료 상태 적용 방식을 유지할 것을
권고합니다.** 방어 가능한 일시 중지는 다섯 군데가 아니라 **열두 군데**의
애플리케이션 접합부를 필요로 하고, 그중 셋은 제품의 모든 저장이 지나가는
경로에 있습니다. 여기에 그래프 안의 영속 표식 파일과 새 UI가 더해집니다.

일시 중지를 진행하기로 결정한다면, 완료 추적(M1)을 **장벽 없이 독립적인
마일스톤으로 먼저** 구축할 것을 권고합니다. 이는 결과 문서가 미해결로
기록한 "실제 보류 저장 캡처 미확립" 문제를 그 자체로 해소하며, 새 권한도
새 UI도 요구하지 않습니다.

**강제되지 않는 유휴 창은 세 번째 선택지가 아닙니다.**

## 범위 밖 (변경 없음)

들어오는 이름 변경과 삭제, 실제 전송, 두 번째 기기, 개인 데이터 등록,
일상 사용과 배포 승인은 범위 밖이며 요청되지 않았습니다.
